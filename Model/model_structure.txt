model_structure

The model is a MCMC exposure model.  
It takes a hypothetical population and runs it through the actual fisheries to estimate likely  exposure.

We estimate the most likely run timing parameters based upon Albion test fishery catch data.

We use a range of migration parameters.
